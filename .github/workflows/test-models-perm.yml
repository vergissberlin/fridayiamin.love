name: Test With Models Permission

on:
  repository_dispatch:
    types: [test-models]

permissions:
  contents: write
  models: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from models permission test"